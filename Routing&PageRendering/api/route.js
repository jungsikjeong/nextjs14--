// route는 다양한 함수를 내보내는 함수임 EX) GET,POST, PUT, DELETE..

export function GET(req) {
  console.log(re);

  // return Response.json();
  return new Response('Hello!');
}

// export function POST(req) {}
